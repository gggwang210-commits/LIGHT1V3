# Security policy

LIGHT ONE V3는 비의료 PT 상담 리포트의 **로컬 파일럿 MVP**입니다. 운영 보안이 검증된 서비스가 아닙니다.

## 지원 범위

| 영역 | 보안 수정 대상 |
|---|---|
| 기본 브랜치의 활성 앱 `lightone_v2_django/` | 지원 |
| `docs/`, `tests/`, `.github/`의 현재 기준 파일 | 지원 |
| `legacy/`, `legacy-docs/`, `lightone-main/`, `lightone_django/` 등 보관 영역 | 실행·배포 지원 안 함 |

과거 구현에서 발견한 문제도 활성 앱에 영향을 주거나 비밀정보·개인정보 노출과 관련되면 제보 대상입니다.

## 공개 저장소 범위

- 코드, 합성 fixture, 비식별 예시 문서만 공개합니다.
- 실제 회원 DB, 이미지, 연락처, 상담 원문, 인증정보, `.env`는 커밋하지 않습니다.
- 합성 식별자는 `SYN-*`, `데모회원-*` 형식만 사용합니다.
- SQLite는 로컬 개발 전용이며 저장소에서 추적하지 않습니다.

## 설정 원칙

- `SECRET_KEY`는 모든 환경에서 필수입니다.
- `DEBUG` 기본값은 `False`입니다.
- 로컬 데모는 `mysite.settings_local`, 운영 구성은 `mysite.settings_production`으로 분리합니다.
- 합성 시드는 명시적인 로컬 `DEBUG=True`에서만 실행됩니다.
- 운영 설정은 PostgreSQL, HTTPS, 보안 쿠키, HSTS를 요구하지만 실제 배포 검증은 별도 절차입니다.

## 취약점 제보

공개 이슈에는 비밀정보나 개인정보를 게시하지 마세요. GitHub 저장소의 **Security → Report a vulnerability** 기능이 제공되면 해당 비공개 경로를 우선 사용합니다. 사용할 수 없다면 저장소 소유자의 GitHub 프로필에 안내된 비공개 연락 경로로 먼저 연락하고, 공개 이슈에는 상세 내용을 남기지 마세요.

제보에는 영향을 받는 경로·버전, 예상 영향, 최소 재현 절차와 완화 제안을 포함할 수 있습니다. 재현에는 실제 사용자 데이터를 사용하지 말고 `SYN-*` 합성 식별자만 사용하세요. 수신 확인이나 수정 기한은 별도 합의 전까지 보장되지 않습니다.

## 한계

이 정책은 저장소 수준의 예방 통제입니다. 키 순환, 접근권한 검토, 로그·백업, 침해 대응, 개인정보 영향평가, 법률 검토는 운영 전 별도로 완료해야 합니다.

운영 전 판단 항목은 [`docs/release-checklist.md`](docs/release-checklist.md)를 함께 확인하세요.
